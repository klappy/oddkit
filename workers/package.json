{
  "name": "oddkit-mcp-worker",
  "version": "0.14.2",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev --var ODDKIT_VERSION:$(node -p \"require('../package.json').version\")",
    "deploy": "wrangler deploy --var ODDKIT_VERSION:$(node -p \"require('../package.json').version\")",
    "tail": "wrangler tail",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "fflate": "^0.8.2"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250124.0",
    "typescript": "^5.7.0",
    "wrangler": "^4.0.0"
  }
}
